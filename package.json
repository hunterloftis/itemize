{
  "name": "itemize",
  "version": "1.0.0",
  "engines": {
    "node": ">=7.4.0"
  },
  "scripts": {
    "lint": "standard --fix itemize.js",
    "test": "yarn run lint && node --harmony node_modules/.bin/jest"
  },
  "main": "itemize.js",
  "license": "MIT",
  "devDependencies": {
    "express": "^4.14.1",
    "jest": "^18.1.0",
    "standard": "^8.6.0"
  },
  "dependencies": {
    "cheerio": "^0.22.0",
    "requisition": "^1.7.0"
  }
}
